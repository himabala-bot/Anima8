import React from 'react';
import ReactDOM from 'react-dom/client';
import Anim8Studio from './app/page';
import './index.css';

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <Anim8Studio />
  </React.StrictMode>
);
